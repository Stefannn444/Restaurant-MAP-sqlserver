<UserControl x:Class="RestaurantAppSQLSERVER.Views.ClientOrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:RestaurantAppSQLSERVER.Views"
             xmlns:vm="clr-namespace:RestaurantAppSQLSERVER.ViewModels"
             xmlns:entities="clr-namespace:RestaurantAppSQLSERVER.Models.Entities"
             xmlns:conv="clr-namespace:RestaurantAppSQLSERVER.Converters"  mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000">

    <d:UserControl.DataContext>
        <vm:ClientOrdersViewModel/>
    </d:UserControl.DataContext>

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <conv:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
        <conv:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="10">
            <TextBlock Text="Istoricul Comenzilor Mele" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap"/>
            <TextBlock Text="{Binding SuccessMessage}" Foreground="Green" TextWrapping="Wrap"/>

            <Button Content="← Inapoi la Meniu"
                    Command="{Binding NavigateBackCommand}"
                    HorizontalAlignment="Left"
                    Margin="0,0,0,10"/>

        </StackPanel>

        <ScrollViewer Grid.Row="1" Margin="10">
            <ItemsControl ItemsSource="{Binding Orders}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type entities:Order}">
                        <Border BorderBrush="LightGray" BorderThickness="1" Padding="10" Margin="0,0,0,10" CornerRadius="3">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{Binding OrderCode, StringFormat='Comanda: {0}'}" FontWeight="Bold" FontSize="16"/>
                                        <TextBlock Text="{Binding OrderDate, StringFormat='Data: {0:dd.MM.yyyy HH:mm}'}" FontSize="12"/>
                                        <TextBlock Text="{Binding Status, StringFormat='Status: {0}'}" FontSize="12"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                        <TextBlock Text="{Binding TotalPrice, StringFormat='Total: {0:C2}'}" FontWeight="Bold" FontSize="16"/>
                                        <TextBlock Text="{Binding TransportCost, StringFormat='Transport: {0:C2}'}" FontSize="12"/>
                                        <TextBlock Text="{Binding DiscountPercentage, StringFormat='Reducere: -{0:C2}'}" FontSize="12" Foreground="Green"/>
                                        <TextBlock Text="{Binding Subtotal, StringFormat='Subtotal: {0:C2}'}" FontSize="12"/>
                                    </StackPanel>
                                </Grid>

                                <Separator Margin="0,10"/>

                                <TextBlock Text="Itemi Comandati:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <ItemsControl ItemsSource="{Binding OrderItems}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type entities:OrderItem}">
                                            <Grid Margin="5,0,0,2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="{Binding Name}" VerticalAlignment="Center"/>
                                                <TextBlock Grid.Column="1" Text="{Binding Quantity, StringFormat='x {0}'}" VerticalAlignment="Center" Margin="10,0"/>
                                                <TextBlock Grid.Column="2" Text="{Binding TotalPrice, StringFormat='C2'}" VerticalAlignment="Center" FontWeight="SemiBold" Margin="10,0"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
